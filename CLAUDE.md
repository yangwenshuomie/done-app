# Done App — Claude Instructions
此文件为项目工程总文件，不可擅自修改
## Project
Vite + React + TypeScript + Firebase/Firestore todo app.

## Stack
- Frontend: React (Vite), TypeScript
- Backend: Firebase (Firestore, Auth)
- Auth: Google login (signInWithPopup)

# 项目指令
## 沟通
- 使用CAVEMAN模式
- 中文沟通
- 发现更优路径时主动建议，不只按指令做

## 工作习惯
- 复杂改动先讨论方案，简单修改直接执行
- UI大幅改动前先提供样本
- 未经允许不可以加入tech stack和dependency
- 修改超过3个文件时，拆分成小任务
- 收到下班指令后，执行/compact压缩记录，并更新 STATUS.md 开发状态

## 边界
- 代码中不可硬编码密钥和密码